{% for sidebar in page.header.sidebars.links %}
	{% set front =  page.find('/components/links/' ~ sidebar).header %}
	{% set links =  front.links %}
	<section>
		<header class="major">
			<h2>{{ front.title }}</h2>
		</header>
		{{ dump(links) }}
		<ul class="default">
			{% for link in front.links %}
				<li><a href="{{ link.target }}">{{ link.text }}</a></li>
			{% endfor %}
		</ul>
	</section>
{% endfor %}
